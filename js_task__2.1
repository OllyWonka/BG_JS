<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Homework</title>
</head>
<body>
<!--
Обязательное задание.

Для кода вставленного в теге script необходимо дать комментарии, почему мы
получаем такой результат (в свободной форме).

Подсказка. Чтобы лучше разобраться возьмите этот код и запустите в отладчике в
браузере со включенными точками остановки.
-->
<script>
  "use strict";

  let a = 1, b = 1, c, d;

  /*
  Почему переменная `c` хранит данное значение?

  Переменная 'a' содержит унарный оператор преинкремента, соответственно значение переменной увеличится на 1: a == 2.
  Поскольку значение переменной 'a' присваивается переменной 'c', c == 2.
   */
  c = ++a;
  console.log(c);

  /*
  Почему переменная `d` хранит данное значение?

  Переменной `d` присваивается изначальное значение переменной `b`, поскольку унарный оператор преинкремента `b` 
  находится в постфиксной форме.
   */
  d = b++;
  console.log(d); // d = 1

  /*
  Почему переменная `c` хранит данное значение?

  Унарный инкремент переменной увеличит ее значение на 1: a == 3.
  Значение переменной `c` образуется путем сложения 2 и переменной `a`.
   */
  c = 2 + ++a;
  console.log(c); // c = 5

  /*
  Почему переменная `d` хранит данное значение?

  Унарный инкремент переменной `b` увеличит ее значение на 1: b == 3.
  Но, поскольку инкремент переменной `b` находится в постфиксной форме, в сложение значения переменной `d` пойдет изначальное 
  (до преобразования) значение переменной `b`, а именно 2.
   */
  d = 2 + b++;
  console.log(d); // d = 4

  console.log(a);
  console.log(b);
</script>
</body>
</html>
